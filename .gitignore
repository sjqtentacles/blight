/target
Cargo.lock

# Benchmark outputs (regenerable via bench/game.sh and bench/run.sh)
/bench/game-results.json
/bench/game-*.json
/bench/games/**/*.json
/bench/*-results.json

# Compiled example/probe binaries and build artifacts
*.o
*.ll
*.dSYM/

# cargo-mutants output
/mutants.out/
/mutants.out.old/

# Scratch file the S1 self-host demo (examples/selfhost_check.bl) writes when run from the repo root
/selfhost_scratch.bl
playground/dist/
