(* Near-empty program: the OCaml native runtime startup/RSS floor, subtracted from each problem's peak
   RSS to give the startup-adjusted "RSS delta" in bench/game.sh. *)
let () = print_string "0\n"
